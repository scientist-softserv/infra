apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "efs.fullname" . }}-test-connection"
  labels:
    {{- include "efs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "efs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

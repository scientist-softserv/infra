apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: test-sm-2
spec:
  refreshInterval: 10m
  secretStoreRef:
    name: aws-secretmanager
    kind: ClusterSecretStore
  target:
    name: test-values-2
    creationPolicy: Owner
    template:
      data:
        bmuConfig: "{{ .config }}"
        values.yaml: "{{ .config | fromJson | toYaml }}"
  data:
   - secretKey: config
     remoteRef:
       key: main
       property: test-values-2

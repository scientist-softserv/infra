apiVersion: mariadb.mmontes.io/v1alpha1
kind: Backup
metadata:
  name: mariadb-fotf
spec:
  mariaDbRef:
    name: mariadb-fotf
  schedule:
    cron: "5 12 * * *"
  maxRetentionDays: 5
  storage:
    persistentVolumeClaim:
      resources:
        requests:
          storage: 500Gi
      accessModes:
        - ReadWriteOnce

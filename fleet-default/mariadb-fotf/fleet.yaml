defaultNamespace: mariadb-fotf
helm:
  chart: .
  releaseName: mariadb-fotf
  valuesFrom:
    - secretKeyRef:
        name: mariadb-fotf-values
        namespace: mariadb-fotf
        key: values.yaml
overrideTargets:
  - clusterSelector:
      matchLabels:
        name: r2-gc
dependsOn:
 - name: main-fleet-default-mariadb-fotf-pre

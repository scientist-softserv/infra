defaultNamespace: redis
helm:
  repo: oci://registry-1.docker.io/bitnamicharts/redis
  chart: redis
  takeOwnership: true
  releaseName: redis
  valuesFiles:
    - redis-values.yaml
overrideTargets:
  - clusterSelector:
      matchLabels:
        name: r2-gc

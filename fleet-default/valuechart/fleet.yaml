helm:
  chart: simple-chart
  valuesFrom:
    - secretKeyRef:
        name: mysecret
        namespace: default
        key: values.yaml
  values:
    name: clark k.
    # override secret?
    longnames:
      english:
        name: original
